# TraceTriage for Android

Three screens over the same measurements the console shows, and the same endpoint it measures
with. There is no mobile backend and no mobile copy of the data: the app reads
`/data/queue.json`, `/data/cards.json` and `/data/provenance.json` from the deployment and
posts to `/api/live/`, so a number on a phone and the same number on the web cannot disagree.

| Screen | What it does |
|---|---|
| **Queue** | The 407 ranked passes, highest review value first, with the reason codes verbatim from `artifacts/QUEUE_RECEIPT.json` |
| **Pass** | The fitted Doppler corridor drawn over the waterfall it was fitted to, with the offset in Hz and ppm |
| **Live** | Post any SatNOGS observation id and the endpoint downloads that waterfall and fits a corridor to it while you wait |

The Pass screen is the one worth opening. A list of scores is a list of scores; the question a
reviewer has is whether the corridor sits on the trace, and that is a picture.

The Live screen answers in one of three ways and all three are real. UNCORRECTED and CORRECTED
come with an offset in Hz and ppm and a p-value over the nulls. UNRESOLVED withholds the offset,
because the image did not settle which shape it holds and a number measured against the wrong
hypothesis means nothing. UNRESOLVED is the common answer: of the 2,727 passes in
`artifacts/GATE3_POOL.json`, 2,291 do not resolve, against 308 UNCORRECTED and 128 CORRECTED.
The first suggested id resolves at 17.06 sigma, so a first tap shows a measurement rather than
three dashes.

An earlier draft of this paragraph said to expect UNRESOLVED always, and justified it with the
largest corridor sigma across the 25 cards, 3.1. That was the wrong quantity. The corridor
fit's sigma and the mode's come from different estimators over different hypotheses, and
observation 14740031 has a corridor sigma of 2.02 and a mode sigma of 25.1.

The endpoint allows six measurements per minute from one caller and answers a seventh with
`RATE_LIMITED` and the seconds to wait. The app prints that code and that number, which it did
not always do: the first version read the refusal at the wrong nesting level and reported
`HTTP_429` over "the endpoint answered without a measurement and without a reason" while the
endpoint had named both. `tests/fixtures/live_api_refusals.json` holds the two bodies as
captured from the deployment so the parser is checked against the real shape.

## Install

Download `tracetriage.apk` from the [latest release](../../releases/latest) and open it.
Android will ask you to allow installing from this source, because this is not a Play Store
listing. Minimum Android 7.0.

Check what you downloaded before you trust it:

```
sha256sum tracetriage.apk                       # compare with the release notes
apksigner verify --print-certs tracetriage.apk  # compare with the fingerprint below
```

Signing key SHA-256:

```
98:B5:30:88:7C:39:6B:D9:78:0B:1F:48:71:54:61:3D:B3:B6:B5:76:17:94:D7:B7:37:83:4A:9C:0B:A1:A8:0D
```

That fingerprint is the public half of a 4096-bit RSA key held outside this repository. It is
published so a download can be checked, and pinned in `build-apk.ps1` so a build signed by
anything else fails before it can be released rather than after.

Android only. On iOS the console installs as a PWA instead: Share, then Add to Home
Screen, and the eight pages work with no network after the first load. That is measured, in
`artifacts/OFFLINE_RECEIPT.json`.

## Build it yourself

```
npm install
npx expo prebuild --platform android
powershell -ExecutionPolicy Bypass -File build-apk.ps1
```

`android/` is generated by the second command and is not tracked, so the release signing
config lives in `plugins/with-release-signing.js` rather than in the generated file: a
hand-edited `build.gradle` survives until the next prebuild and then silently reverts to the
public debug key. With no keystore present the build still works and produces a debug-signed
APK, which is what a fork needs; `build-apk.ps1` then says so rather than letting it pass as a
release.

Three things that will waste an afternoon otherwise:

- **`JAVA_HOME` must be a JDK between 17 and 21.** Android Studio ships one at
  `<install>/jbr`, which is what `build-apk.ps1` uses. A system Java 22 fails with an
  unsupported class file version, which reads like a corrupt dependency.
- **`apksigner` is per build-tools version.** The script picks the newest one that has it,
  because an SDK accumulates several and the oldest is usually the one on `PATH`.
- **A long checkout path decides what this app may depend on.** React Native's codegen
  writes object file names that embed the full source path, so from a deep directory
  `react-native-safe-area-context` produces a 268-character name and ninja fails with
  `Filename longer than 260 characters`. Building through a junction does not help, because
  CMake canonicalises the path before ninja sees it. So the system-bar inset is computed in
  JavaScript from
  `StatusBar.currentHeight` instead, and `src/App.tsx` states the one approximation that
  costs: the bottom inset is a constant, correct for gesture navigation and 24px short on a
  three-button device.

Running the APK is what found that inset. The first build printed "TraceTriage" through the
status-bar clock and "3/6 GATES MET" through the battery icon, on a bundle that compiled with
`tsc` clean and every test passing. No check in this repository looks at a rendered screen, so
nothing caught it until somebody opened the app.

## What is checked, and where

`tests/test_mobile_client.py` in the repository root pins the properties that make this the
same product as the console rather than a lookalike: the nine palette values against
`apps/web/app/globals.css`, the origin against the console's own `metadataBase`, the trailing
slash on `/api/live/`, that all three screens are reachable, and that the signing plugin is
still registered. `npx tsc --noEmit` covers the rest.

No screenshot in this file. A screenshot of an app is a claim about a build that may not be
the one you downloaded, and the APK is right there.
